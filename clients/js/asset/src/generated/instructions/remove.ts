/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  ExtensionType,
  ExtensionTypeArgs,
  getExtensionTypeSerializer,
} from '../types';

// Accounts.
export type RemoveInstructionAccounts = {
  /** Asset account */
  asset: PublicKey | Pda;
  /** The authority of the asset */
  authority?: Signer;
  /** The asset defining the group, if applicable */
  group?: PublicKey | Pda;
  /** The account receiving refunded rent */
  recipient?: PublicKey | Pda;
};

// Data.
export type RemoveInstructionData = {
  discriminator: number;
  extensionType: ExtensionType;
};

export type RemoveInstructionDataArgs = { extensionType: ExtensionTypeArgs };

export function getRemoveInstructionDataSerializer(): Serializer<
  RemoveInstructionDataArgs,
  RemoveInstructionData
> {
  return mapSerializer<RemoveInstructionDataArgs, any, RemoveInstructionData>(
    struct<RemoveInstructionData>(
      [
        ['discriminator', u8()],
        ['extensionType', getExtensionTypeSerializer()],
      ],
      { description: 'RemoveInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 16 })
  ) as Serializer<RemoveInstructionDataArgs, RemoveInstructionData>;
}

// Args.
export type RemoveInstructionArgs = RemoveInstructionDataArgs;

// Instruction.
export function remove(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RemoveInstructionAccounts & RemoveInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'asset',
    'AssetGtQBTSgm5s91d1RAQod5JmaZiJDxqsgtqrZud73'
  );

  // Accounts.
  const resolvedAccounts = {
    asset: {
      index: 0,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 2,
      isWritable: false as boolean,
      value: input.group ?? null,
    },
    recipient: {
      index: 3,
      isWritable: true as boolean,
      value: input.recipient ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.recipient.value) {
    resolvedAccounts.recipient.value = context.identity.publicKey;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveInstructionDataSerializer().serialize(
    resolvedArgs as RemoveInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
