/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type HandoverInstructionAccounts = {
  /** Asset account */
  asset: PublicKey | Pda;
  /** The authority of the asset */
  authority?: Signer;
  /** The new authority of the asset */
  newAuthority: Signer;
};

// Data.
export type HandoverInstructionData = { discriminator: number };

export type HandoverInstructionDataArgs = {};

export function getHandoverInstructionDataSerializer(): Serializer<
  HandoverInstructionDataArgs,
  HandoverInstructionData
> {
  return mapSerializer<
    HandoverInstructionDataArgs,
    any,
    HandoverInstructionData
  >(
    struct<HandoverInstructionData>([['discriminator', u8()]], {
      description: 'HandoverInstructionData',
    }),
    (value) => ({ ...value, discriminator: 15 })
  ) as Serializer<HandoverInstructionDataArgs, HandoverInstructionData>;
}

// Instruction.
export function handover(
  context: Pick<Context, 'identity' | 'programs'>,
  input: HandoverInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'asset',
    'AssetGtQBTSgm5s91d1RAQod5JmaZiJDxqsgtqrZud73'
  );

  // Accounts.
  const resolvedAccounts = {
    asset: {
      index: 0,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    newAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.newAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getHandoverInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
